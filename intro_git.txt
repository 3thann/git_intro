git init

git add file  -> le nom du fichier
	*         -> tout les fichiers sans extensions
	.         -> tout les fichiers

git commit -m "{msg}"

git push

(git pull recupérer depuis github)


git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin {link}
git push -u origin main

git branch {nom} -> créé une branche
git checkout {nom} -> change de branche
git branch -d {nom} -> supp la branche
